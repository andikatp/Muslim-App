import 'package:flutter/material.dart';

class ZakatTradePage extends StatelessWidget {
  const ZakatTradePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text('Kalkulator Zakat Niaga'),
      ),
    );
  }
}
